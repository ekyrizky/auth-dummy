jdk:
  - openjdk11  # Ensure you're using JDK 11 or a version compatible with your code

android:
  build: false  # Skip default Android build, because we're using Flutter's .aar file
  module: "."    # Use the current directory for the module
